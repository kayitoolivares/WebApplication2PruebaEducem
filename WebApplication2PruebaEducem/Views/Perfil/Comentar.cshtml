@model WebApplication2PruebaEducem.Controllers.xxModel

@{
    ViewBag.Title = "Comentar";
}

<h2>Comentar</h2>

    <div class="row">
        <div class="col col-sm-8">

            <h2>Nota</h2>
            <small>

               

                @Html.DisplayFor(model => model.List1.usuario.personal.nombre)
                    @Html.DisplayFor(model => model.List1.usuario.personal.apematerno)
                    @Html.DisplayFor(model => model.List1.usuario.tipo)
                <br />
               
                @Html.DisplayFor(model => model.List1.fecha)
            </small>
            <p>
                <h4>
                  
                    @Html.DisplayFor(model => model.List1.contenido)
                </h4>
            </p>
            <p>

            </p>

        </div>
    </div>
    <div class="row">
        <div class="col col-sm-1"></div>
        <div class="col col-sm-8">
            <form action="/Perfil/CreateComentario/@Html.DisplayFor(model => model.List1.id)" method="post">
                <div class="form-floating">
                    <textarea class="form-control" maxlength="250" placeholder="Leave a comment here" name="contenido"></textarea>
                    <label for="floatingTextarea">Comentario</label>
                </div>

                <button class="btn btn-success">Crear</button>
            </form>
        </div>
    </div>

<div class="row">
    @foreach (var item in Model.List2)
    {

        @*<div class="card">
                <div class="card-header">
                    @item.fecha
                </div>
                <div class="card-body">
                    @item.usuario.personal.nombre

                    @item.usuario.personal.apematerno
                    @item.usuario.tipo
                    <h5 class="card-title">Nota</h5>
                    <p class="card-text">@item.contenido</p>
                    <a href="/Perfil/Comentar" class="btn btn-primary">Comentar</a>
                </div>
            </div>*@


        <div class="col col-sm-12">
            <h2>Comentario</h2>
            <small>
                @item.usuario.personal.nombre @item.usuario.personal.apematerno @item.usuario.tipo
                <br />
                @item.fecha
            </small>
            <p>
                <h4>    @item.contenido</h4>
            </p>

        </div>


    }


</div>

